
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4
      
      - name: Setup Node.js 🔧
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'
          
      - name: Install Dependencies 📦
        run: npm ci
        
      - name: Build 🏗️
        run: npm run build
        
      - name: Deploy to GitHub Pages 🚀
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          cname: raptihandmadepapers.com.np
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          commit_message: 'Deploy to GitHub Pages'

# Alternative deployment method if GITHUB_TOKEN still has issues
# Uncomment the following job if needed and add a GH_PAT secret to your repository
# deploy-with-pat:
#   needs: build
#   runs-on: ubuntu-latest
#   steps:
#     - name: Deploy with Personal Access Token
#       uses: peaceiris/actions-gh-pages@v3
#       with:
#         personal_token: ${{ secrets.GH_PAT }}
#         publish_dir: ./dist
#         cname: raptihandmadepapers.com.np
